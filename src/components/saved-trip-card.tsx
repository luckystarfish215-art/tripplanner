import type { SavedTrip } from "@/types/saved-trip";

type SavedTripCardProps = { destinationName: string; onDelete: (id: string) => void; trip: SavedTrip };

export function SavedTripCard({ destinationName, onDelete, trip }: SavedTripCardProps) {
  return <article className="rounded-2xl border border-white/10 bg-white/[.045] p-5"><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-medium tracking-tight text-white">{destinationName}</h2><p className="mt-1 text-sm text-white/50">{trip.startDate} — {trip.endDate}</p></div><span className="rounded-xl bg-pink-300/10 px-3 py-2 text-sm font-semibold text-pink-100">{trip.confidenceScore}/100</span></div><div className="mt-4 flex flex-wrap gap-2 text-xs text-white/55"><span className="rounded-full bg-white/5 px-2.5 py-1">{trip.travelStyle}</span><span className="rounded-full bg-white/5 px-2.5 py-1">{trip.companion}</span><span className="rounded-full bg-white/5 px-2.5 py-1">{trip.budget}</span></div><div className="mt-5 flex flex-col gap-2 sm:flex-row"><a className="flex min-h-11 flex-1 items-center justify-center rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-black transition hover:bg-pink-100" href={`/trip/${trip.id}`}>Reopen trip</a><a className="flex min-h-11 flex-1 items-center justify-center rounded-xl border border-white/15 px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-white/10" href={`/trip/${trip.id}`}>Share link</a><button aria-label={`Delete ${destinationName} trip`} className="min-h-11 rounded-xl border border-red-200/20 px-4 py-2.5 text-sm font-semibold text-red-100 transition hover:bg-red-300/10" onClick={() => onDelete(trip.id)} type="button">Delete</button></div></article>;
}
