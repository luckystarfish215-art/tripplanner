import { Icon } from "@/components/icon";
export function SiteHeader({ brand, navigation }: { brand: string; navigation: string[] }) { return <header className="site-header"><a className="brand" href="#top"><span className="brand-mark"><Icon name="sparkle" /></span>{brand}</a><nav aria-label="Main navigation" className="hidden gap-7 text-sm text-white/55 sm:flex">{navigation.map((item) => <a className="transition hover:text-white" href="#planner" key={item}>{item}</a>)}</nav><button aria-label="Open navigation" className="menu-button sm:hidden" type="button"><span /><span /></button></header>; }
